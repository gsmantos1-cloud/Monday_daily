# Porta do servidor (padrão: 3001)
PORT=3001

# Chave secreta para os tokens JWT — mude isso em produção!
JWT_SECRET=team-hub-s3cr3t-k3y-2024

# (Opcional) Chave da API Anthropic para geração de tarefas com IA
# Obtenha em: https://console.anthropic.com/
# Sem esta chave, o sistema usa templates pré-definidos por setor
ANTHROPIC_API_KEY=sk-ant-sua-chave-aqui
